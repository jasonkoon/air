{
  "name": "air",
  "version": "1.0.0",
  "description": "air compressor valve controller",
  "main": "app.ts",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-pm2": "tsc && pm2 start dist/server.js && pm2 save"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^15.12.4",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.3.4"
  },
  "dependencies": {
    "onoff": "^6.0.3"
  }
}
